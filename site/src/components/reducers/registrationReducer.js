const initialState = {
  email: '',
  password: '',
  isAuth: '',
  isLoading: '',
};

const registrationReducer = (state = initialState, action) => {
  switch (action.type) {
    case XXX:
      return;
    default:
      return state;
  }
};
